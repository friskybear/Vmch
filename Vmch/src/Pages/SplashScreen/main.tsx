import React from "react";
import ReactDOM from "react-dom/client";
import SC from "./SplashScreen";

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
  <React.StrictMode>
    <SC />
  </React.StrictMode>
);
